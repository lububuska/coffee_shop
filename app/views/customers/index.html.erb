<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Покупатели</title>
</head>
<body>
  <h1>Покупатели</h1>
  <div class="container">
    <table>
      <thead>
        <tr>
          <th>Имя</th>
          <th>Почта</th>
          <th>Номер телефона</th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <% @customers.each do |customer| %>
          <tr>
            <td><%= customer.name %></td>
            <td><%= customer.email %></td>
            <td><%= customer.phone %></td>
            <td><%= link_to 'Показать', customer, class: "btn btn-show" %></td>
            <td><%= link_to 'Редактировать', edit_customer_path(customer), class: "btn btn-edit" %></td>
            <td><%= link_to 'Удалить', customer, method: :delete, data: { confirm: 'Вы точно хотите удалить клиента?' }, class: "btn btn-destroy" %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <!-- Кнопки справа -->
    <div class="buttons">
      <%= link_to 'Новый покупатель', new_customer_path, class: "btn btn-new" %>
    </div>
  </div>
</body>
</html>